sudo: false

language: python

matrix:
  include:
    - python: 2.7
    - python: 3.4
    - python: 3.5
    - python: 3.6
    - python: 3.7
      dist: xenial
      sudo: true
  fast_finish: true

install:
  - pip install codecov

script:
 - "export PYTHONPATH=.:vendor"
 - "python -m unittest discover tests"

after_success:
  - codecov
